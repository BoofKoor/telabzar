# تل‌ابزار — Telabzar

رباتِ مدیریت فایلِ تلگرام: هر فایلی بفرست، نوعش تشخیص داده می‌شود و یک منوی عملیاتِ اینلاینِ تمیز می‌گیری.
معماریِ **توزیع‌شدهٔ Master/Node** (پخشِ بارِ پردازش و پهنای‌باند)، **وبهوک از ابتدا**، و **نصبِ تعاملی**.

> **وضعیت:** Milestone 1 از نقشهٔ راه — اسکلت + وبهوک + Installer.
> پردازشِ واقعیِ فایل‌ها از M2 اضافه می‌شود.

---

## این نسخه (M1) چه می‌کند
- ربات با **وبهوک** بالا می‌آید (از طریقِ سرورِ محلیِ Bot API — بدونِ نیاز به دامنه).
- `/start` **دوزبانه** (فارسی/انگلیسی) با انتخابِ زبان.
- تشخیصِ **نوعِ فایل** (سند/تصویر/ویدیو/صوت/آرشیو) و نمایشِ **کارتِ عملیاتِ اینلاین**.
- زیرساخت: `local-bot-api` · `postgres` · `redis` · `bot` روی Docker Compose.
- نصبِ تعاملی که مقادیر را می‌پرسد (بدونِ ویرایشِ دستیِ `.env`).

عملیاتِ روی دکمه‌ها فعلاً پیامِ «به‌زودی (M2)» می‌دهند.

## پیش‌نیازها
- یک سرور (VPS) با Docker و Docker Compose.
- توکنِ ربات از [@BotFather](https://t.me/BotFather).
- `api_id` و `api_hash` از [my.telegram.org](https://my.telegram.org).

## نصب
```bash
git clone <repo-url> telabzar && cd telabzar
bash install.sh
```
installer مقادیر را می‌پرسد، `.env` را با اسرارِ تصادفی می‌سازد، و استک را بالا می‌آورد.
سپس در تلگرام به ربات `/start` بده.

### دستورهای روزمره
```bash
telabzar status     # وضعیت سرویس‌ها
telabzar logs bot   # لاگ زنده
telabzar update     # pull + rebuild
telabzar reconfigure
```

## معماری
- **Master** = مغز (وبهوک، پنلِ ادمینِ وب، زمان‌بند، دیتابیس) + کارگرِ محلی.
- **Node** (از M5) = کارگر + پهنای‌باند؛ از پنلِ ادمینِ وب اضافه می‌شود، روی WireGuard.
- صف: **ARQ** (Redis) · MTProto برای لینک/استریم: **Kurigram**.

## نقشهٔ راه
| Milestone | محتوا |
|---|---|
| **M1** | اسکلت + وبهوک + Installer ← این نسخه |
| M2 | هستهٔ پردازش + صفِ ARQ + عملیاتِ پایه |
| M3 | امنیت (اسکن، ساندباکس، سهمیه) |
| M4 | عملیاتِ سند/موزیک/تصویر/ویدیو/آرشیو |
| M5 | لایهٔ توزیع‌شده: نودها + پنلِ ادمینِ وب |
| M6 | Delivery توزیع‌شده (لینک/استریم) |
| M7 | صیقل + فیچرهای خلاقانه + LLM |

## ساختار
```
app/            کدِ ربات (aiogram 3.30)
  routers/      هندلرها (start, files)
  locales/      کاتالوگِ دوزبانه
docker/         Dockerfile
docker-compose.yml
install.sh      نصبِ تعاملی
```

## توسعه
پایتون ۳.۱۲، aiogram 3.30، SQLAlchemy async + Postgres، Redis (FSM).
