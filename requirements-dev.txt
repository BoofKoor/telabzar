# وابستگی‌های تست (نه در هیچ ایمیجِ اجرایی). اجرا: pip install -r requirements-dev.txt && pytest
-r requirements.txt
pytest>=8,<10
pytest-asyncio>=0.24,<2
aiohttp>=3.9,<4
fakeredis>=2.20,<3
# `app.processing`/`app.tasks` سرِ import به PIL نیاز دارند (از requirements-worker)
Pillow>=10,<12
# درایورِ async SQLite برای تست‌های DB (گاردِ مالکیت). تولید روی Postgres است؛
# SQLite فقط این‌جاست تا تست بدونِ سرویسِ بیرونی اجرا شود.
aiosqlite>=0.20,<1
# مسیرِ socksِ موتورِ `direct` با یک سرورِ SOCKS5ِ واقعیِ درون‌پروسه‌ای تست می‌شود،
# پس همان کتابخانه‌ای که تولید استفاده می‌کند این‌جا هم لازم است (نه ماکِ کانکتور).
aiohttp_socks>=0.10,<1
# گاردهای compose باید **به تفکیکِ سرویس** بخوانند: تطبیقِ رشته‌ایِ
# `COOKIES_DIR: /cookies` صرفاً چون سرویسِ دیگری آن را دارد سبز می‌شود، یعنی
# گاردِ بی‌اثر. YAML را با پارسر می‌خوانیم نه با regex.
PyYAML>=6,<7
